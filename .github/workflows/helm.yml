name: Helm chart

on:
  pull_request:
    paths:
      - charts/**
      - .github/workflows/helm.yml
  push:
    branches: [main]
    paths:
      - charts/**
      - .github/workflows/helm.yml

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
      - run: helm lint charts/screenshot-studio
      - run: helm template screenshot-studio charts/screenshot-studio > rendered-chart.yaml
